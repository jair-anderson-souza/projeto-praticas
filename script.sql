create table game(
    id int auto_increment,
    nome varchar(30),
    genero varchar(30),
    primary key(id)
);
create table PESSOA(
    id int auto_increment,
    email varchar(30),
    senha varchar(30),
    primary key(id)
);